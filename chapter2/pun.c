#include <stdio.h>

// compile + link: cc pun.c
// Rename the executable cc -o <name> pun.c
// GGC compiler. To compile gcc -o pun pun.c

int main(void)
{
    printf("To C, or not to C: that is the question.\n");
    return 0;
}